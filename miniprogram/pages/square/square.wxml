<view class="post" wx:for="{{post_list}}" wx:for-item='item' wx:for-index="index" wx:key='id'>
  <van-image round width="4rem" height="4rem" src="{{item.avatar_url}}" />
  <text>{{item.nickname}}</text>
</view>